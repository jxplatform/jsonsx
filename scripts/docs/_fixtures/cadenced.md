---
title: "A file with a cadence problem"
---

# A file with a cadence problem

| Frame | Count |
| ----- | ----- |
| Something rather than something else, not a cell that counts | 1 |

## A heading that says rather than, and is not counted

The adapter resolves from the host rather than the project, and the project rather than the
default. That is a decision, not a fallback.

```md
This fence says rather than, and is frozen, not counted.
```
