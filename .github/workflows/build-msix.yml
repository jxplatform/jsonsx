name: Build Windows Store Package (MSIX)

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build-msix:
    name: Build MSIX
    runs-on: windows-latest

    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Install Windows SDK
        run: |
          choco install windows-sdk-10-version-2004-all -y --no-progress
          echo "C:\Program Files (x86)\Windows Kits\10\bin\10.0.19041.0\x64" >> $env:GITHUB_PATH

      - name: Build workspace packages
        run: bun run build

      - name: Build MSIX package
        run: bun run desktop:msix

      - name: Upload MSIX artifact
        uses: actions/upload-artifact@v7
        with:
          name: msix-package
          path: packages/desktop/artifacts/*.msix
          if-no-files-found: error
          retention-days: 30

      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.ref_name }}
          draft: false
          files: packages/desktop/artifacts/*.msix
