{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -f package.json ] && grep -q '\"jxsuite\"' package.json; then bun .claude/hooks/pre-bash.js; fi"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash|WriteFile|EditFile",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -f package.json ] && grep -q '\"jxsuite\"' package.json; then bunx nano-staged --unstaged --quiet --bail || exit 2; fi"
          }
        ]
      }
    ]
  }
}
