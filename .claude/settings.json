{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -f package.json ] && grep -q '\"nano-staged\"' package.json; then bunx nano-staged --unstaged --quiet --bail || exit 2; fi"
          }
        ]
      }
    ]
  }
}
